user: GitHub Actions workflow in YAML. Name: Cross-Platform CMake CI. Trigger on push/pull_request to branches containing 'develop' or 'master'. Create a job 'cross_build' that uses a strategy matrix to run on 'ubuntu-latest', 'windows-latest', and 'macos-latest'. The steps must check out the code, run the CI script ci.sh or ci.bat depending on the OS, and then execute a deploy step.


copilot: Why: I'll add a todo entry for the GitHub Actions workflow and then create/update the workflow file ci.yml so the repository runs cross-platform CI on push and PR for branches containing "develop" or "master". Expected outcome: todo updated and workflow file added.

Completed (5/6) Add Unix CI script

I'll create and write the workflow file ci.yml with the required matrix and OS-specific steps. After creating it I'll run a quick syntax check by printing the file content.

ci.yml+50-1
Now I'll open the newly added workflow file to quickly validate its contents.

Read ci.yml

Progress update: I added the workflow file ci.yml and verified its contents. Next, I'll mark the GitHub Actions todo as completed.

Опис змін

Додано файл ci.yml з назвою "Cross-Platform CMake CI".
Тригер: push і pull_request для гілок, що містять 'develop' або 'master'.
Job cross_build використовує matrix: ubuntu-latest, windows-latest, macos-latest.
Кроки:
Checkout (actions/checkout@v4)
Підготовка середовища для Linux/macOS (встановлення cmake; brew для macOS)
Запуск ci.sh на Linux/macOS і ci-windows.bat на Windows
Порожній placeholder-крок Deploy (виводить повідомлення) — замініть на реальні кроки деплою за потреби.